#include <stdio.h>
int main()
{
   //การรับ input ด้วย fgets
   char str[10]; // เผื่อพื้นที่
   gets(str);
   printf("in = %s",str);
   fgets(str,buffer,stdin)
   
   false
   int i=0;
   float f=0.0;
   char c= '\0'; 
    terminate character , null character -> ascii 0
    "dee"=['d','e','e','\0(null character)'] 
    // ระวังถูกกินพื้นที่

    //true
    //"dee"=['d','e','e','\0(null character)'] 
    fgets(str,10,stdin);
    int i=atoi(str);

    // Relational Operator > < >= <= == !=
    // logical Op. && ! ||
    if (i>400){
        printf("if"); }
    else {
        printf("else");
    }

}