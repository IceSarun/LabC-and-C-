#include <stdio.h>

 int main(){
     int num,i=0;
    
     scanf("%d",&num);
     int a[num];
     for (i = 0; i < num; ++i) {
        scanf("%d", &a[i]);
    }
    for(i = 0; i < num; ++i){
        
    }
 }
